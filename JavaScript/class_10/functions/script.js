/*
Syntax:-
function functionName(parameters) {
    // code to be executed[body]
}

functionName(arguments);
*/

function welcome(name) {
  console.log(`Welcome to ${name}`);
}

// welcome();
// welcome();
// welcome();
// welcome();
// welcome();
// welcome();
// welcome();

let studentName = prompt("Enter Student Name: ");
welcome(studentName); //Argument